# 2026-03-31 Nightly Build Log

## Built: Real API Usage Tracker

**Task:** Replace hardcoded cost/request estimates in morning dashboard with real tracked data.

### What was built
- `~/clawd/scripts/log_api_call.py` — SQLite-backed API usage tracker
  - `log_call(service, tokens_in, tokens_out, cost, metadata)` — log one API call
  - `get_monthly_stats(service)` → `{calls, cost, tokens_in, tokens_out}` for current month
  - `get_all_monthly_stats()` → dict of all services with monthly stats
  - DB: `~/clawd/data/api_usage.db`

- `~/clawd/projects/morning-dashboard/fetch_data.py` — `load_api_status()` updated
  - Now pulls real data from the tracker DB
  - Falls back to 0 if service has no data yet
  - Service list / status checks unchanged

### Status
- ✅ Both files committed
- ✅ DB initialized and tested (test inserts deleted on restart — normal)
- ⚠️ DB starts empty — will populate as Jarvis logs actual calls going forward
- **Next step:** Add `log_call()` calls throughout the codebase wherever we hit external APIs (Grok, TickTick, Gmail, etc.)

### Pending from yesterday
- Networking scanner ran — report sent (Telegram channel unavailable at cron time, TickTick task created ✅)
- Prospector DB: ~100 leads may still have "contacted" status reset — needs LinkedIn cross-reference
- Prospector backup cron: not yet set up
- Gemini API key: still needs rotation
- Grok: still timing out on API calls
- Whoop tokens: missing, needs re-auth
